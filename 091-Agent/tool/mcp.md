

mcp 定义：MCP 是一种工具面向 LLM 暴露能力的标准化协议（规范格式），用于统一工具的发现、描述、调用和结果返回方式，使不同来源的工具都能以一致的方式被 LLM 使用。

解决问题：不同工具缺乏统一的能力暴露和调用方式，导致每接入一种工具都需要单独适配，难以复用和共享。

额外功能：  之所以额外功能是，mcp主要的作用是 规范协议，而不是 跨服务的提供tool。
MCP Server 提供 Tool

MCP Client 负责发现、连接和调用 MCP Server

LLM 或 Agent 通常通过 MCP Client 使用这些 Tool