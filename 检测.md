# 后端服务观测体系完整知识框架

## 一、核心四大观测维度（总纲）

**日志 Logs + 指标 Metrics + 链路 Trace + 告警 Alert**
定位问题：查日志 → 看指标 → 追链路 → 收告警

---

## 二、日志观测层（记录行为与异常）

### 1. 日志分级

- DEBUG：调试详情
- INFO：正常业务流程
- WARN：轻微异常、潜在风险
- ERROR：业务错误、接口失败
- FATAL：服务崩溃、致命错误
  
  ### 2. 日志分类
1. **访问日志**
   接口请求路径、请求方式、IP、UA、请求ID、状态码、整体耗时
2. **业务日志**
   入参/出参、用户行为、数据增删改查、AI调用记录
3. **错误日志**
   程序panic、空指针、数据库异常、第三方调用失败、权限异常
4. **慢日志（性能核心）**
- 服务接口慢请求日志
- MySQL慢查询日志：超时SQL、全表扫描、无索引SQL
- Redis慢命令日志：大Key、阻塞命令
5. **系统日志**
   服务启停、配置加载、定时任务、进程状态
   
   ### 3. 日志规范
   
   统一携带：**TraceID、SpanID、用户ID、环境、服务名**
   格式结构化：JSON格式，便于检索筛选
   
   ### 4. 常用工具
   
   ELK、Loki、Graylog、GORM日志、Zap/Logrus

---

## 三、指标监控层（量化系统状态）

### 1. 服务器基础设施指标

- CPU：使用率、负载、上下文切换
- 内存：使用率、剩余内存、OOM风险
- 磁盘：使用率、磁盘IO、读写压力
- 网络：带宽、TCP连接数、丢包、延迟
  
  ### 2. 应用进程指标（Go后端重点）
- 协程数量、线程数、Goroutine泄漏
- GC频率、GC停顿耗时、堆内存占用
- 文件句柄数、连接池空闲/繁忙数量
  
  ### 3. 接口业务流量指标
- QPS/TPS：每秒请求量、峰值流量
- 响应耗时：P50/P90/P99分位耗时
- 错误率：5xx服务错误、4xx参数错误
- 并发数：实时活跃请求数、最大并发
  
  ### 4. 中间件核心指标
  
  #### MySQL
  
  活跃连接数、空闲连接数、读写QPS、事务提交/回滚数
  锁等待时长、死锁次数、索引命中率、慢SQL数量、缓存命中率
  
  #### Redis
  
  内存占用、内存淘汰Key、命令QPS、缓存命中率
  客户端连接数、阻塞等待、大Key数量
  
  #### 消息队列
  
  消息堆积量、生产速率、消费速率、消费失败率
  
  ### 5. 自定义业务指标
  
  注册量、访问量、搜索量、AI调用次数、业务成交数据等
  
  ### 6. 常用监控组件
  
  Prometheus + Grafana、InfluxDB、SolarMan、云服务监控

---

## 四、链路追踪层（全请求流程溯源）

### 1. 核心作用

追踪**一次完整请求**全链路耗时，精准定位性能瓶颈

### 2. 链路流转路径

客户端 → 网关 → 业务服务 → MySQL → Redis → 第三方服务(LLM/HTTP)

### 3. 可观测内容

1. 每一段调用耗时占比
2. 慢接口、慢SQL、慢缓存、慢第三方调用定位
3. 跨服务调用异常、超时、级联故障
4. 串联：接口总耗时 = 业务逻辑 + 数据库 + 缓存 + 外部调用
   
   ### 4. 主流技术栈
   
   标准协议：**OpenTelemetry**
   可视化组件：Jaeger、SkyWalking、Zipkin
   
   ### 5. 开发接入要点
   
   全局透传 TraceID，日志、指标、链路三者打通联动

---

## 五、告警预警层（风险前置）

### 1. 资源类告警

CPU/内存/磁盘使用率过高、服务器负载异常

### 2. 服务可用性告警

服务宕机、实例下线、健康检查失败、端口不通

### 3. 接口业务告警

接口错误率飙升、P99耗时突增、流量突降/突增

### 4. 数据库告警

慢SQL暴增、连接数打满、死锁频发、事务大量回滚

### 5. 缓存/队列告警

Redis连接异常、队列消息大量堆积、消费阻塞

### 6. 告警渠道

控制台、企业微信、钉钉、邮箱、短信
---

## 六、数据库专项观测（你当前重点）

1. 开启**慢查询日志**，设置超时阈值
2. 日常使用 `EXPLAIN` 观测三类问题
   - 索引失效：`type=ALL` 全表扫描
   - 回表查询：无 `Using index`
   - 文件排序：出现 `Using filesort`
3. 观测维度：SQL执行频率、平均耗时、扫描行数、索引命中情况
4. 优化闭环：观测慢SQL → Explain分析 → 优化SQL/索引 → 复测观测

---

## 七、Go后端项目观测落地优先级

1. 基础：结构化日志 + 接口耗时打印
2. 进阶：MySQL慢日志观测 + Explain常态化校验
3. 实战：接入Prometheus基础指标
4. 高阶：OpenTelemetry全链路追踪
5. 成型：配置自动化告警体系

---

## 八、面试精简背诵版

1. 日志：记录行为异常，排查报错与业务流程
2. 指标：量化系统性能、流量、资源水位
3. 链路：追踪单次请求全流程，定位慢节点
4. 告警：主动发现隐患，提前处理线上故障
5. 数据库观测核心：抓慢SQL、查执行计划、优化索引与SQL
