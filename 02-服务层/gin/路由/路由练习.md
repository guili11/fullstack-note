进阶实战练习题：企业级 Gin 路由 + 8 个中间件 + 多层分组嵌套（难度拉满，贴合真实项目）
这次我直接按生产环境标准出题：新增跨域、请求耗时、全局异常捕获、接口限流、敏感接口校验等真实必备中间件，加入多层嵌套分组、单个路由独立中间件、多中间件组合，彻底练透路由 + 中间件的所有用法！
一、终极题目要求
实现一个电商后台管理系统，严格遵循企业开发规范：
1. 必写中间件（8 个，全覆盖生产场景）
表格
中间件	作用	挂载范围
全局日志中间件	打印请求路径	全局所有接口
请求耗时统计中间件	打印接口执行耗时	全局所有接口
跨域中间件	解决前端跨域问题	全局所有接口
全局异常捕获中间件	统一处理程序 panic 崩溃	全局所有接口
登录校验中间件	验证用户登录态	私有接口分组
管理员权限中间件	验证管理员身份	管理员分组
接口限流中间件	防止接口被恶意请求	开放 / 管理员分组
敏感接口校验中间件	高危接口二次校验	单个独立路由
2. 路由分组规范（5 组，多层嵌套）
公开接口 /auth：无任何校验（登录、注册）
开放接口 /open：限流保护（公告、验证码）
用户业务接口 /api/v1：必须登录（用户、订单、商品模块）
管理员接口 /admin：登录 + 管理员 + 限流（用户管理、数据统计）
敏感操作接口 /admin/user/delete：额外加独立中间件
二、完整接口清单
1. 公开组 /auth（无中间件）
POST /auth/login 登录
POST /auth/register 注册
2. 开放组 /open（挂载：限流中间件）
GET /open/notice 系统公告
GET /open/captcha 获取验证码
3. 用户业务组 /api/v1（挂载：登录校验）
用户子组 /api/v1/user：GET /info
订单子组 /api/v1/order：GET /list
商品子组 /api/v1/goods：GET /list
4. 管理员组 /admin（挂载：登录 + 管理员 + 限流）
用户管理子组 /admin/user：GET /list、DELETE /delete（加敏感中间件）
数据统计子组 /admin/stat：GET /total
三、直接复制所有中间件（8 个，模拟生产逻辑）
go
运行
package main

import (
	"log"
	"time"
	"github.com/gin-gonic/gin"
)

// 1. 全局日志中间件
func LoggerMiddleware() gin.HandlerFunc {
	return func(c *gin.Context) {
		log.Printf("【请求】%s %s", c.Request.Method, c.Request.URL.Path)
		c.Next()
	}
}

// 2. 请求耗时统计中间件
func CostMiddleware() gin.HandlerFunc {
	return func(c *gin.Context) {
		start := time.Now()
		c.Next()
		cost := time.Since(start)
		log.Printf("【耗时】%s 耗时: %v", c.Request.URL.Path, cost)
	}
}

// 3. 跨域中间件（生产必备）
func CorsMiddleware() gin.HandlerFunc {
	return func(c *gin.Context) {
		c.Header("Access-Control-Allow-Origin", "*")
		c.Header("Access-Control-Allow-Methods", "GET,POST,PUT,DELETE")
		c.Next()
	}
}

// 4. 全局异常捕获中间件
func RecoveryMiddleware() gin.HandlerFunc {
	return func(c *gin.Context) {
		defer func() {
			if err := recover(); err != nil {
				c.JSON(500, gin.H{"msg": "服务器异常", "error": err})
				c.Abort()
			}
		}()
		c.Next()
	}
}

// 5. 登录校验中间件
func AuthMiddleware() gin.HandlerFunc {
	return func(c *gin.Context) {
		token := c.GetHeader("token")
		if token != "admin123" {
			c.JSON(401, gin.H{"msg": "请先登录"})
			c.Abort()
			return
		}
		c.Next()
	}
}

// 6. 管理员权限中间件
func AdminMiddleware() gin.HandlerFunc {
	return func(c *gin.Context) {
		role := c.GetHeader("role")
		if role != "admin" {
			c.JSON(403, gin.H{"msg": "无管理员权限"})
			c.Abort()
			return
		}
		c.Next()
	}
}

// 7. 接口限流中间件（模拟）
func RateLimitMiddleware() gin.HandlerFunc {
	return func(c *gin.Context) {
		// 模拟：直接放行，仅做标记
		log.Println("【限流】接口限流校验通过")
		c.Next()
	}
}

// 8. 敏感接口校验中间件（单独给删除接口用）
func SensitiveMiddleware() gin.HandlerFunc {
	return func(c *gin.Context) {
		key := c.GetHeader("safe-key")
		if key != "123456" {
			c.JSON(403, gin.H{"msg": "敏感操作，校验失败"})
			c.Abort()
			return
		}
		c.Next()
	}
}
四、你的核心任务（必须完成）
注册4 个全局中间件（日志、耗时、跨域、异常捕获）
创建 5 个路由分组，完成多层嵌套
给对应分组批量挂载多个中间件
给单个路由独立挂载敏感中间件
所有接口返回标准 JSON 格式