我给你**纯考前极简复习笔记**，无废话、无原理套话、只留「是什么+怎么写+注意事项」，**明天笔试直接翻这一篇就够**，从最简单到高频排序。

## 一、暴力枚举（所有算法基础）
**适用**：数据量很小、挨个遍历比对
**写法**：单层/双层 for 循环

**注意事项**
- 数据量大必超时，别硬写暴力
- 嵌套循环注意变量名不要混（i, j, k 很容易写串）
- break / continue 确认跳出的是哪一层

## 二、哈希算法（笔试送分题）
**适用**：统计次数、去重、两数之和、判断存在
**Go 模板**
```go
m := make(map[int]int)
for _,v := range nums {
    m[v]++
}
```
**核心**：空间换时间，查找 O(1)

**注意事项**
- map 必须用 `make` 初始化，nil map 不能直接赋值
- 读不存在的 key 返回零值，用 `_, ok := m[key]` 判断是否存在
- 遍历 map 顺序随机，不要依赖顺序

## 三、双指针（最高频！）
**适用**：有序数组、回文串、反转、两数之和
### 1. 左右指针（回文、有序数组）
```go
l,r := 0,len(s)-1
for l < r {
    // 判断逻辑
    l++
    r--
}
```
### 2. 快慢指针（链表、去重）

**注意事项**
- `l < r` 退出后，l 和 r 不需要再处理（已经碰过头了）
- 指针移动方向确认清楚，左指针右移、右指针左移
- 快慢指针中，快指针走两步不要忘了判空 `fast.Next != nil`
- 数组下标越界是双指针最常见的 bug

## 四、滑动窗口（连续子数组/子串）
**适用**：最长子串、连续和
**核心**：右指针一直扩，满足条件左指针缩

**注意事项**
- 窗口收缩时先判断左指针指向的值再移动，顺序不要错
- 字符串用 `s[l]` 取到的是 byte，不是 rune（中文会乱）
- 窗口内状态要随着 l/r 移动同步更新，容易漏

## 五、二分查找（有序数组专属）
**适用**：有序数组找数、找边界
**口诀**：有序才能二分

### 标准模板
```go
l, r := 0, len(nums)-1
for l <= r {
    mid := l + (r-l)/2  // 防溢出
    if nums[mid] == target {
        return mid
    } else if nums[mid] < target {
        l = mid + 1
    } else {
        r = mid - 1
    }
}
```

**注意事项**
- **退出条件 `l <= r`**：`l > r` 时退出，l 是第一个大于 target 的位置，用 `<` 会漏最后一个元素
- **mid 防溢出**：用 `l + (r-l)/2`，不用 `(l+r)/2`
- **更新必须是 mid ± 1**：直接赋 mid 会死循环
- **左右边界分情况**：不同模板处理不同，别混用
- **无序数组不能用二分**

## 六、字符串专项（你明天必考！！）
### 核心三套固定写法
1. 按空格分割（ACM万能）
```go
arr := strings.Fields(line)
```
2. 按指定字符分割（IP）
```go
parts := strings.Split(s, ".")
```
3. 字符转数字
```go
num, err := strconv.Atoi(part)
```

**注意事项（必考坑点）**
- 前导零非法（`"01"` 不行，`"0"` 可以）
- 空串处理：split 后可能有空串，要 `len(part) > 0` 判断
- 长度超限：转 int 后可能溢出（尤其大数场景）
- `strconv.Atoi` 返回的 err 必须判断，不判断直接拿 num 用会出 bug
- 字符串是 byte 数组，遍历中文用 `[]rune(s)` 转一下

## 七、BFS 层序遍历（树必考，你刚吃透）
**适用**：层序遍历、最短路径
**固定满分模板**
```go
queue := []*Node{root}
res := [][]int{}

for len(queue) > 0 {
    size := len(queue)
    level := []int{}

    for i:=0;i<size;i++ {
        node := queue[0]
        queue = queue[1:]

        level = append(level, node.Val)
        queue = append(queue, node.Children...)
    }
    res = append(res, level)
}
```

**注意事项**
- **出队用 `queue = queue[1:]`，不准用 `:=`**，否则没改到原 slice
- **先存 `size := len(queue)`**，内层循环不能用 `len(queue)`，队列长度会变
- **入队判空**：`node.Children` 可能为 nil，但 append 没问题
- **入队即标记**：网格 BFS 中，找到合法格子立刻标记已访问，杜绝重复入队
- **边界判断前置**：先判坐标越界，再访问数组
- **BFS 保证最短路径**：第一次到达终点直接返回

## 八、DFS 深度遍历（简单递归）
**适用**：树的前中后序、简单路径查找
**模板**
```go
func dfs(root *Node) {
    if root == nil {
        return
    }
    // 处理当前节点
    for _,c := range root.Children {
        dfs(c)
    }
}
```

**注意事项**
- **临界条件**：`if root == nil { return }` 必须写，否则栈溢出死亡
- **`return` 是子问题的解**：若 dfs 有返回值（如求树高），父 dfs 需要接收并利用；若只是遍历，void return 仅表示终止
- **`*res` 是全局共享标志**：需要跨递归层收集结果时，用指针参数 `res *[]int`，所有递归层共享同一个变量
- **局部变量不要放参数里**：如 `path []int`，在递归函数内部声明为局部变量，每层独立副本；除非需要回溯共享才放参数
- **前序 vs 后序**：代码位置决定语义。前序 = 先处理当前节点再递归子节点（从上到下）；后序 = 先递归子节点再处理当前节点（从下到上）
- **DFS vs BFS**：DFS 只管"能否走到"，不保证最短路径；求最短步数用 BFS

## 九、动态规划（只背保命1题，其余全放弃）
### 爬楼梯（小厂唯一DP必考）
```go
func climbStairs(n int) int {
    if n<=2 {return n}
    a,b := 1,2
    for i:=3;i<=n;i++{
        a,b = b,a+b
    }
    return b
}
```

**注意事项**
- **n <= 2 特判**：不要忘，否则数组越界
- **f(n) = f(n-1) + f(n-2)**：和斐波那契区别只是初始值不同
- **其他 DP 题直接放弃**，不浪费时间

## 十、ACM 万能输入模板（明天笔试最最最重要）
```go
package main
import (
    "bufio"
    "fmt"
    "os"
    "strconv"
    "strings"
)

func main() {
    sc := bufio.NewScanner(os.Stdin)
    for sc.Scan() {
        s := sc.Text()
        arr := strings.Fields(s)
        // 你的题目逻辑写这里
    }
}
```

**注意事项**
- **多组输入**：必须用 `for sc.Scan()` 循环，不能只读一行
- **类型转换**：`sc.Text()` 拿到 string，转 int 用 `strconv.Atoi`
- **空行跳过**：`len(s) == 0` 时 `continue`
- **err 必须判**：`Atoi` 返回的 err 不判断会出 bug

---

# 🔥 最终考前优先级（做题顺序）
1. **字符串题**（必拿满分）
2. **数组/哈希/双指针**（送分）
3. **BFS层序遍历**（稳拿分）
4. **简单二分**
5. **DP只写爬楼梯**
6. **复杂图论/高级DP/贪心直接放弃**

# 终极避坑3条（你专属易错点）
1. BFS 出队 **不准用 `:=`**
2. 字符串判断 **必须校验前导零 + 空串 + Atoi err**
3. 笔试默认 **多组输入 `for sc.Scan()`**
