# 滑动窗口核心笔记（固定长度 + 不定长 统一梳理）

完全基于你的理解整理，核心突出**两种窗口的指针规则、执行顺序差异**，是最贴合你思维的记忆模板

***

## 一、滑动窗口 通用核心本质

所有滑动窗口的底层逻辑完全一致：
**维护一个动态/固定的窗口，通过调整窗口的左右边界，持续检查窗口是否满足题目要求**

***

## 二、两大类型 核心区别（你总结的关键点）

1. **指针移动**：固定窗口 `l` 自动计算，不定长窗口 `l/r` 按条件手动增减
2. **执行顺序**：扩张、缩减、检查的顺序会根据窗口类型改变
3. **窗口大小**：固定窗口长度不变，不定长窗口长度动态变化
4. **窗口初始化**：固定滑动，需要先初始化，再开始遍历

***

## 三、固定长度滑动窗口（异位词/定长子串）

### 适用场景

窗口大小**固定不变**（如：长度为k的子串、字母异位词）

### 1. 指针规则（核心）

- 右指针 `r` 遍历起点 = **窗口固定长度**
- 左指针 **不手动维护**，通过公式计算：`left = r - 窗口长度`
- 天然保证窗口长度恒定，无需判断 `r-l`

### 2. 标准执行顺序（不可颠倒）

**扩张 → 缩减 → 检查**

1. 扩张：将右指针新元素加入窗口
2. 缩减：移除左指针旧元素，维持固定长度
3. 检查：判断当前窗口是否满足条件

### 3. 初始化流程

1. 构建目标参照标准（哈希/数组）
2. 提前装填**第一个完整窗口**
3. 检查第一个窗口

***

## 四、不定长滑动窗口（无重复子串/最长子串）

### 适用场景

窗口大小**动态变化**（如：最长无重复子串、最短子数组）

### 1. 指针规则（核心）

- 左右指针 `l、r` 都从 **0 开始**
- `r` 一直向右遍历（主动扩张）
- `l` 根据**题目条件**动态右移（被动缩减）
- 完全依靠条件控制指针，不固定窗口长度

### 2. 标准执行顺序（顺序改变）

**扩张 → 检查是否缩减 → 统计**

1. 扩张：右指针一直右移，加入新元素
2. 缩减：**满足条件时，左指针才移动**（核心：按条件缩窗口）  
3. 统计： 核心就是 统计 邻接点的 数据结果，  那就判断是 在 缩减前，还是缩减后了  （反正记住，统计时的窗口必须符合条件   其实就是最长 最短导致的， ）

***

## 五、终极对比记忆表

| 维度     | 固定长度滑动窗口     | 不定长滑动窗口        |
| ------ | ------------ | -------------- |
| 窗口大小   | 固定不变         | 动态变化           |
| 右指针起点  | 窗口长度         | 0              |
| 左指针维护  | 公式自动计算       | 按条件手动右移        |
| 核心执行顺序 | 扩张 → 缩减 → 检查 | 扩张 → 条件缩减 → 检查 |
| 典型题目   | 异位词、定长子串     | 最长无重复子串、最短子数组  |

***

## 六、你的核心总结（极简背诵版）

1. **滑动窗口 = 维护窗口 + 检查条件**
2. **固定窗口**：r定起点，l自动算，先扩再缩再检查
3. **不定窗口**：lr都从0开始，扩张一直， 缩减看条件  ，结果统计时机  看 题目
4. 二者本质一样，区别仅在**指针移动规则**和**扩缩查顺序**

