# MySQL 索引核心八股（纯题目版）
---

## 一、SQL 执行基础（新增）
一条 SQL 语句从客户端发送到 MySQL，完整的执行流程是什么？
SQL 语句的逻辑执行顺序是什么？
MySQL 查询优化器是什么，核心作用是什么？
## 一、索引基础
1.  InnoDB 为什么用 B+ 树做索引？
2.  聚簇索引和二级索引的核心区别是什么？
3.  什么是回表？什么是覆盖索引？

## 二、联合索引（核心）
1.  什么是联合索引的最左前缀原则？
2.  范围查询（`> / BETWEEN`）为什么会截断后续索引字段？
3.  联合索引字段排序的核心规则是什么？（等值/范围/排序的优先级）

## 三、索引失效
1.  列举 3 种最常见的索引失效场景。
2.  `!= / NOT IN` 一定不走索引吗？

## 四、EXPLAIN 执行计划
1.  `type` 字段从优到劣的关键取值有哪些？
2.  `Extra` 里出现 `Using filesort`/`Using temporary` 代表什么？
3.  `rows` 字段代表什么？

## 五、优化取舍
1.  索引越多越好吗？为什么？
2.  什么是索引下推（ICP）？