1.数据库表设计    - 满足业务+一定性能要求    (关联如何实现  字段如何存储  索引)
2.建表               - automigrate（容易误改数据库，并且没有版本管理）？还是手动（执行 init.sql）         如何手动呢？  (分环境呗，要么宿主机，要么docker 要么云服务)       
3.连接数据库   - 怎么连接，怎么控制连接？
4.操作数据库 （gorm的使用）  gorm操作教程  事务？  





建表语句 写好 init.sql   



制作可迁移的 go项目 ：

数据库连接：

检测到数据库不存在err后自动创建数据库，并且执行建表语句   db.Exec() 



repo层书写原则  原则上一个repo函数只允许执行一个 sql操作，多个需要事务控制，事务控制则需要在 service中执行





gorm的使用：

gorm的使用：建立models ，然后就开始db.xxx()使用咯 



怎么保证一个用户只有一个 简历？ 单表  自然是

多表  

数据库层面  简历表 



```go
// ApplicationRepo 投递记录仓库接口
type ApplicationRepoInterface interface {
	Create(app *domain.Application) error
	List(positionID uint64, candidateID uint64, page int, pageSize int) ([]domain.Application, int64, error)
	UpdateStatus(id uint64, status string) error
}

type ApplicationRepo struct {
	db *gorm.DB
}

```

dfffd



gorm具体操作

写好接口，方便查阅，工厂模式。



增删改查   操作类型分为两类  查  增删改

查是需要返回结果的，

操作执行也分为两类，

1.一路链式直接操作

2.先拿到query，然后再执行操作（同一数据，不同结果，总数  list）
